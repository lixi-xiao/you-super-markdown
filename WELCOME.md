# 👋 欢迎来到 You Super Markdown

嗨，欢迎！如果你刚把这套系统部署到服务器，或者第一次打开这个项目，这份文档就是给你准备的。

简单说：**这是一个轻量级的在线 Markdown 阅读网站**——把 Markdown 文章放上去，大家就能在浏览器里舒舒服服地阅读、评论、听歌、换背景。而且它被"全副武装"过，安全性比一般的小网站强很多。

你不用懂太多技术就能把它跑起来。下面用大白话带你走一遍。

---

## 🎯 它能干嘛（核心功能）

- **发文章**：支持 Markdown 语法，写起来很爽，渲染出来更爽
- **评论互动**：访客可以评论，可以回复、点赞，还能设置频率限制防止刷屏
- **听音乐**：内置 QQ 音乐 / 网易云音乐播放，阅读的时候来点 BGM
- **换背景**：图片、API、纯色都行，还能加模糊和透明度调节
- **分角色管理**：不是"管理员说了算"的一言堂，有五个层级的权限体系

---

## 🛡️ 它有什么特别（主要特点）

这是它和普通小网站最不一样的地方——**五层防线**：

| 防线 | 大白话解释 |
|------|-----------|
| 隐藏入口 | 管理员登录入口是动态生成的，每次不一样，猜不到也扫不到 |
| 角色分权 | 超管 / 站长 / 写作者 / 用户 / 访客，各管各的，谁也别想越权 |
| 审计日志 | 所有关键操作都记账，而且账本做了防篡改（哈希链），想赖账没门 |
| 文件守护 | 有人偷偷改了网站文件？守护进程几秒钟就把它还原回去 |
| 数据隔离 | 用户数据、日志等敏感文件对公网完全不可见 |

v2.3.0 还多了一个好玩的：**蜜罐联动**。服务器上挂了个"假目标"（Hfish 蜜罐）引诱攻击者上钩，谁碰了蜜罐、攻击次数够了（默认 3 次），自动封禁它的登录/注册/评论。

---

## 💻 跑起来需要什么（部署环境）

说实话要求很低，一台小破服务器都行：

- **系统**：Ubuntu 20.04+ / Debian 11+ / CentOS 8+
- **PHP**：8.0 以上（要带 fpm、json、mbstring、curl 这几个扩展）
- **Nginx**：1.18 以上
- **Python**：3.8 以上（守护进程要用，记得装 watchdog 库）
- **磁盘**：1GB 以上就够

---

## 🚀 快速上手（基本操作流程）

### 1. 部署（一条命令）

```bash
sudo bash ym-install.sh
```

脚本会一路帮你搞定：装依赖、放文件、建管理员、配 Nginx、装证书、开防火墙……中途会让你输入域名、邮箱、Web 根目录。装完终端会打印：

```
网站地址：https://你的域名
管理入口：https://你的域名/admin/entry/一串随机字符
一次性密码：一串随机密码
```

**⚠️ 这三行只显示一次，立刻截图或记下来！** 丢了就得重新生成。

### 2. 登录超管后台

```bash
sudo ym-admin login        # 生成新的登录入口 + 一次性密码
```

终端会给你一个入口 URL 和一个 OTP（10 分钟有效、只能用一次）。打开 URL、输入 OTP，就进超管后台了。

### 3. 日常管理

- **发文章**：超管 / 站长 / 写作者都能发，权限各不同
- **管人**：超管建站长，站长建写作者，一层管一层
- **看安全**：超管后台能看登录日志、审计日志、蜜罐攻击记录
- **改样式**：网站标题、背景、注册开关、评论开关都在"系统配置"里

### 4. 升级更新

1. 后台"在线更新"上传更新包
2. SSH 执行 `sudo ym-admin challenge` 拿挑战码（**300 秒有效**，不用像以前那样赶时间）
3. 后台输入挑战码，SSH 执行 `sudo ym-admin apply-update`
4. 完事，刷新页面看新版本

---

## ⚠️ 几个要注意的坑（关键注意事项）

这些是踩过坑总结出来的，记住能省很多事：

1. **一次性密码都是"一次性"的**：OTP 用过即废，入口 10 分钟过期；挑战码 300 秒过期、用完作废。过期了重新生成就行，别慌。
2. **别删 `data/` 目录**：用户、文章、日志、封禁都在里面。更新包打包时故意**排除 `data/`**（保护你的数据），所以升级不会动它——这也是为什么升级后网站标题还是旧名字（标题存在 data 里），去后台"系统配置"改一下就新了。
3. **别手痒去改线上文件**：守护进程盯着 14 个核心文件，你手动改可能被它当"被篡改"秒级还原。要改就改本地源码 → 打包 → 走更新流程。
4. **更新包只放一个地方**：统一放 `f:\测试白盒\update-package`，别在源码目录里建七七八八的打包文件夹。
5. **蜜罐别乱开**：蜜罐是故意暴露的"诱饵"，攻击者碰到才会被记录。正常使用者不会碰它，放心。
6. **命令前要不要加 sudo**：凡是"写操作"（建账号、备份、更新、改配置）都要 `sudo ym-admin xxx`；只读的（看状态、验日志）不用。

---

## 📦 项目结构速览

```
f:\测试白盒\
├── README.md                # 正式使用指南（想看细节翻它）
├── DEVELOPMENT_GUIDELINES.md # 开发规范（团队协作用）
├── test_development\        # 主库（源码 + Git 管理）
├── test_development copy\   # 备份副本
└── update-package\          # 更新包统一存放处
```

---

好了，介绍就到这里。先部署起来玩一玩，遇到问题再翻 README 或者回来问我。祝你用得愉快！🎉
