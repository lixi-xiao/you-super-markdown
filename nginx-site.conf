# You Super Markdown Nginx 站点配置【示例模板】
# 提示：一键安装脚本（ym-install.sh）会自动生成正式配置（含 Let's Encrypt 证书路径），
# 本文件仅供手动部署参考——复制前请替换：
#   your-domain.example.com → 你的真实域名
#   phpX.Y-fpm.sock        → 服务器实际 PHP 版本（Ubuntu 24.04 为 php8.3-fpm.sock）
server {
    listen 80;
    server_name your-domain.example.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_tokens off;
    server_name your-domain.example.com;

    ssl_certificate     /etc/letsencrypt/live/your-domain.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.example.com/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256;
    ssl_prefer_server_ciphers off;

    root /var/www/you-markdown;
    index index.php index.html;

    # 安全响应头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # 禁止访问数据目录
    location ~ ^/data/.*\.json$ {
        deny all;
        return 403;
    }

    # 禁止访问 SQLite 数据库（含 WAL/SHM 文件）
    location ~ ^/data/ym\.db(-wal|-shm)?$ {
        deny all;
        return 403;
    }

    # 禁止访问脚本/配置/备份等敏感文件
    location ~* \.(sh|py|conf|bak|sql|log)$ {
        deny all;
        return 403;
    }

    # 禁止访问 CLI/安装/调试文件
    location ~ ^/(ym-admin|ym-install\.sh|ym-guard\.py|ym-hfish-sync\.py|ym-migrate|test\.php|debug\.php|entry_debug\.php|entry_fixed\.php)$ {
        deny all;
        return 403;
    }

    # ACME 验证放行（certbot webroot 续期）
    location ^~ /.well-known/ {
        allow all;
    }

    # 禁止下载应用配置（泄露 repo/hfish 信息）
    location = /app-config.json {
        deny all;
        return 403;
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
        return 403;
    }

    # OTP 动态入口 - 直接传递给 PHP 避免内部重定向改变 REQUEST_URI
    location /admin/entry/ {
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root/admin/entry.php;
        fastcgi_pass unix:/var/run/php/phpX.Y-fpm.sock;
    }

    # 自定义入口路径 fallback（支持站长/写作者自定义路径）
    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    # PHP 处理
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/phpX.Y-fpm.sock;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_read_timeout 300;
    }

    # 静态文件缓存
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff2?|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
